import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryBlue = Color(0xFF1A5CFF); // Ana mavi renk
  static const Color backgroundColor = Color(
    0xFFF2F5F9,
  ); // Sayfa arka planı (açık gri)
  static const Color cardBackground = Colors.white; // Kart arka planı (beyaz)
  static const Color textPrimary = Color(0xFF333333); // Koyu gri metin
  static const Color textSecondary = Color(0xFF666666); // Orta gri metin
  static const Color textLight = Colors.white; // Beyaz metin
  static const Color darkBackground = Color(
    0xFF121212,
  ); // Karanlık mod arka planı
  static const Color textDark = Color(0xFF333333);
  static const Color textMedium = Color(0xFF666666);
  static const Color white = Color(0xFFFFFFFF);
  static const Color buttonColor = Color(0xFF00091F);
}
